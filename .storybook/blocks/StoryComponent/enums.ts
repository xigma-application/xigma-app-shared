export enum ContentAlignItems {
  top = 'top',
  center = 'center',
  bottom = 'bottom',
}

export enum ContentDisplay {
  block = 'block',
  flex = 'flex',
  grid = 'grid',
}

export enum ContentGridFlow {
  column = 'column',
  row = 'row',
  maxTwoColumns = 'max-two-columns',
  maxThreeColumns = 'max-three-columns',
  maxFourColumns = 'max-four-columns',
  maxFiveColumns = 'max-five-columns',
  maxSixColumns = 'max-six-columns',
  maxEightColumns = 'max-eight-columns',
}
